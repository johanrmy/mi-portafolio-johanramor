import { Josefin_Sans, Fugaz_One } from "next/font/google";

// Importar las fuentes con los pesos deseados
export const josefin_sans = Josefin_Sans({
  subsets: ["latin"],
});

export const fugaz_one = Fugaz_One({
  subsets: ["latin"],
  weight: ["400"],
});
